/**
 * 定数の再エクスポート（後方互換性のため）
 * 新しいコードは import { ... } from './constants/index' を使用してください
 */
export { FIXED_PARAMS, defaultVariableParams } from './constants/manufacturing'
export type { VariableParams } from './constants/manufacturing'
